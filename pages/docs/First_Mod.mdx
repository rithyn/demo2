
# Creating your first mod

In the Mod Code window, type the code for your first mod:

```js copy showLineNumbers filename="mod.js"
this.options = {
  root_mode: "survival",
  map_size: 30
};

this.tick = function(game) {
};
```



